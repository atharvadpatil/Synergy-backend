syntax = 'proto3';

package auth;

service AuthService {
    rpc GetUserByEmail (UseEmailRequest) returns (UserResponse);
}

message UseEmailRequest {
    string email = 1;
}

message UserResponse {
    string userId = 1;
    string userName = 2;
    string userEmail = 3;
    string userAvatar = 4;
}